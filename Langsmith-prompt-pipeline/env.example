# ============================================
# LLM 配置 - 选择使用 Azure OpenAI 或 标准 OpenAI
# ============================================
# LLM 提供商类型: azure 或 openai
# 默认: azure
LLM_PROVIDER=azure


# ============================================
# 标准 OpenAI 配置
# ============================================
# 当 LLM_PROVIDER=openai 时使用以下配置

# OpenAI API Key
OPENAI_API_KEY=sk-your_openai_api_key_here

# OpenAI 模型名称（可选，默认 gpt-4o）
OPENAI_MODEL=gpt-4o

# OpenAI API Base URL（可选，默认官方地址）
# OPENAI_API_BASE=https://api.openai.com/v1

# OpenAI 组织 ID（可选）
# OPENAI_ORGANIZATION=your_org_id


# ============================================
# Azure OpenAI 配置
# ============================================
# 当 LLM_PROVIDER=azure 时使用以下配置

# Azure OpenAI 的 Endpoint 地址
AZURE_ENDPOINT=https://your-resource.openai.azure.com/

# Azure OpenAI 的部署名称（模型部署名）
AZURE_DEPLOYMENT=gpt-4o-eus2

# Azure OpenAI API 版本
AZURE_API_VERSION=2024-02-15-preview

# Azure OpenAI API Key
AZURE_API_KEY=your_azure_api_key_here


# ============================================
# LangSmith 配置（可选 - 用于追踪和监控）
# ============================================
# LangSmith API Key
LANGSMITH_API_KEY=your_langsmith_api_key_here

# LangSmith Endpoint（通常使用默认值）
LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# LangSmith 项目名称
LANGSMITH_PROJECT=economic_report


# ============================================
# 搜索工具配置（可选）
# ============================================
# Perplexity API Key（用于联网搜索）
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# Tavily API Key（备选搜索工具，可选）
# TAVILY_API_KEY=your_tavily_api_key_here


# ============================================
# 使用说明
# ============================================
# 1. 复制此文件为 .env
#    Windows PowerShell: Copy-Item env.example .env
#    Windows CMD: copy env.example .env
#    Linux/Mac: cp env.example .env
#
# 2. 根据你的情况选择配置类型：
#
#    方案 A - 使用标准 OpenAI：
#      LLM_PROVIDER=openai
#      OPENAI_API_KEY=你的key
#      OPENAI_MODEL=gpt-4o  (或其他模型)
#
#    方案 B - 使用 Azure OpenAI：
#      LLM_PROVIDER=azure
#      AZURE_ENDPOINT=你的endpoint
#      AZURE_DEPLOYMENT=你的部署名
#      AZURE_API_KEY=你的key
#
# 3. 可选配置：
#    - LANGSMITH_* （不需要追踪功能可不配置）
#    - PERPLEXITY_API_KEY （不需要联网搜索可不配置）
#
# 4. 确保 .env 文件不被提交到 Git

