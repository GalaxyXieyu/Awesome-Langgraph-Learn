## 生产部署、可观测性与 Prompt 工程

---

### 1. 开场引入

#### 引入策略：从「上线恐惧」引入

**开场白建议**：
> "假设你花了两个月做了一个 DeepResearch Agent，本地跑得很好。老板让你明天上线。你突然想到：
> - 系统崩溃了怎么办？有监控吗？
> - API 费用暴涨怎么办？能预警吗？
> - Prompt 里的『系统提示词』改了 20 版，现在用的是哪一版？
> - 用户投诉回答质量差，你能复现当时的情况吗？
> - 开发环境、测试环境、生产环境的配置怎么隔离？
>
> 这就是生产部署的现实。今天的主题是：**从『能跑』到『能上线』**。"

#### 生活化类比：餐厅后厨监控

> "想象你是一家连锁餐厅的老板。后厨里有 20 个厨师同时做菜，你需要知道：
> - **Trace = 订单追踪**：3 号桌的宫保鸡丁现在做到哪一步了？厨师 A 在切菜，厨师 B 在炒菜，预计还有 5 分钟出锅。
> - **Score = 菜品评分**：今天宫保鸡丁的平均评分 4.5 分，但有 3 个客人说太咸。是哪个厨师做的？
> - **Dataset = 顾客反馈收集**：把每天的『差评订单』自动收集到一个本子里，每周复盘改进。
> - **Prompt = 菜谱管理**：宫保鸡丁的菜谱改了 3 版——1 版太辣、2 版太甜、3 版刚好。所有分店都要用第 3 版。
>
> Langfuse 就是后厨的『监控大屏』+『评分系统』+『反馈收集器』+『菜谱管理中心』。"

#### 与 LG-08 的衔接

> "上节课我们解析了 DeepResearch Agent 的架构——它能跑、功能完整。今天我们要让它『能上线』——可监控、可追踪、可回滚、可优化。"

---

### 2. 核心内容

#### 2.1 预构建 Agent 深度模式回顾

**讲解顺序**：
1. `create_react_agent` 的 plan-execute 内部机制回顾
2. 预构建 vs 自定义的选型决策矩阵

---

#### 2.2 缓存策略实战

**讲解顺序**：
1. `CachePolicy` 深化配置
2. 缓存失效策略：TTL、主动失效、版本号
3. 缓存穿透/雪崩防护

---

#### 2.3 Redis + Postgres 混合架构深化

**讲解顺序**：
1. 双后端数据流设计
2. 一致性策略：Cache-Aside、Write-Through
3. 容灾与降级

---

#### 2.4 知识库向量检索与 RAG

**讲解顺序**：
1. 向量存储集成：Chroma/Pinecone
2. RAG 节点设计：retrieve→rerank→inject
3. 检索效果评估与可视化

---

#### 2.5 Langfuse 全链路可观测性

**讲解顺序**：
1. **`init_langfuse_observability()`**：从环境变量初始化
2. **`CallbackHandler`**：自动追踪图执行
3. **`create_callback_handler()`**：为每次执行创建追踪实例
4. **`get_langfuse_config()`**：一键获取 config 字典
5. **Tags 标签系统**：环境、服务、用户维度的标记
6. **Score 评分系统**：缓存命中率、输出质量、响应时间
7. **Dataset 管理**：QA Capture 自动保存运行数据

**必须画 Langfuse Trace 链路图的点**：
```
Trace: 用户查询 "新能源汽车分析"
├── Node: intent_classifier (23ms)
├── Node: mode_router (15ms)
│ └── LLM Call (312ms)
├── Node: task_planner (45ms)
│ └── LLM Call (456ms)
├── Node: search_parallel (1203ms)
│ ├── Subgraph: finance (800ms)
│ ├── Subgraph: news (600ms)
│ └── Subgraph: competitor (750ms)
├── Node: plan_router (18ms)
└── Node: report_generator (2345ms)
└── LLM Call (2300ms)
```

**讲解话术**：
> "Langfuse Trace 就是这样一个『执行家谱』。用户的每次请求是一个 Trace，Trace 里每个节点、每次 LLM 调用、每次工具调用，都是一条记录。你点击任何一条，都能看到当时的输入、输出、耗时、token 用量。
>
> 出了 bug？找到那条 Trace，一步一步看是哪个节点出了问题。"

---

#### 2.6 Score 评分的业务意义

**讲解顺序**：
1. `report_cache_hit_rate()`：上报缓存命中率
2. 自定义评分：输出质量、响应时间、用户满意度
3. Score 时序图：追踪指标随时间的变化

**讲解话术**：
> "Score 不只是『技术监控』，更是『业务指标』。缓存命中率 85% 意味着你省了 85% 的 API 费用；响应时间 P99 < 2s 意味着用户体验达标；输出质量评分下降意味着 Prompt 需要优化。"

---

#### 2.7 Prompt 生命周期管理

**讲解顺序**：
1. **YAML 本地存储**：Prompt 以 YAML 文件管理，支持 Jinja2 和 f-string
2. **Prompt 元数据配置**：版本、标签、适用场景
3. **Langfuse Prompt Hub**：云端托管与版本管理
4. **自动拉取**：本地不存在时从 Hub 自动拉取
5. **推送发布**：本地推送到 Hub，自动打 `labels: ["production"]`
6. **PromptManager API**：`get_prompt()` / `build_langchain_prompt()` / `save_prompt()` / `push_to_hub()`

**必须画 Prompt 工作流图的点**：
```
开发环境 Langfuse Hub 生产环境
┌─────────┐ ┌─────────────┐ ┌─────────┐
│ YAML v1 │ ──push──→ │ Prompt Hub │ ←──pull── │ YAML v1 │
│ (本地编辑)│ │ (版本管理) │ │ (运行时) │
└────┬────┘ └──────┬──────┘ └─────────┘
│ │
↓ ↓
┌─────────┐ ┌─────────────┐
│ YAML v2 │ ──push──→ │ labels: │
│ (迭代中) │ │ ["latest"] │
└─────────┘ │ ["production"]│
└─────────────┘
```

**讲解话术**：
> "Prompt 管理就像『菜谱管理』。你先在本地用 YAML 写菜谱（Prompt），改了几版后觉得 OK 了，推送到云端菜谱库（Langfuse Prompt Hub），打上『production』标签。生产环境的应用自动拉取带 production 标签的最新版。
>
> 要回滚？把上一版打上 production 标签，生产环境自动切回来。要 A/B 测试？v1 打标签给 A 组，v2 给 B 组。"

---

#### 2.8 Docker 容器化与部署

**讲解顺序**：
1. Dockerfile 多阶段构建
2. 健康检查与日志
3. LangGraph Cloud 简介

---

#### 2.9 性能调优与安全

简要覆盖：并发控制、递归限制、状态大小优化、连接池、API 认证、速率限制。

---

### 3. OpsMate：生产级运维助手实战

#### 案例与概念的一一对应

| 概念 | OpsMate 中的体现 |
|------|-----------------|
| 预构建 vs 自定义对比 | 同一查询两种实现，实时展示路径差异 |
| 缓存监控 | 节点级缓存命中/未命中的实时色块矩阵 |
| Redis + Postgres | 双后端健康看板，模拟故障降级 |
| RAG | 运维知识库向量检索，相似度热力图 |
| Langfuse Trace | 每次图执行的完整链路瀑布图 |
| Langfuse Score | 缓存命中率、输出质量、响应时间时序图 |
| Langfuse Dataset | QA Capture 采集的数据集管理 |
| Langfuse Tags | 按环境/服务/用户筛选 Trace |
| Prompt YAML | 本地 Prompt 版本管理 |
| Prompt Hub 同步 | 本地 vs 云端版本差异对比、同步状态灯 |

#### 代码演示的讲解话术

**Step 1 - Langfuse Trace 演示**：
> "看 Langfuse 的 Trace 大屏。这条 Trace 是用户『查服务器 CPU 告警』的请求。点击展开——大家看这个时间瀑布图：意图识别 23ms、模式路由 15ms、LLM 调用 312ms... 总耗时 1.2s。哪个节点最慢？一目了然。"

**Step 2 - Score 看板**：
> "看 Score 看板。昨天的缓存命中率平均 87%，但凌晨 3 点掉到了 45%。点击那个时间点——发现是一次批处理任务触发了大量冷查询。这就是 Score 的业务价值：不只是技术数字，是『成本预警器』。"

**Step 3 - Dataset QA Capture**：
> "这个 Dataset 是自动采集的。每次用户查询 + 系统回答 + 用户反馈（👍/👎）自动保存到这里。我们可以选中差评数据，一键重新运行评估——看改了 Prompt 后，同样的输入输出质量有没有提升。"

**Step 4 - Prompt 同步演示**：
> "看 Prompt 同步看板。左边是本地 YAML 文件的内容，右边是 Langfuse Hub 上的版本。红色高亮表示『本地删除了这行』，绿色表示『本地新增了这行』。同步状态灯是黄色——表示『本地有更新，待推送』。点击一键推送，灯变绿色。"

**Step 5 - 故障模拟**：
> "现在模拟 Redis 故障。看双后端监控面板——Redis 状态从绿色变成红色，系统自动降级到 Postgres。虽然查询延迟从 2ms 变成了 50ms，但服务没有中断。"

#### 关键转折点（需要停下来确认学员理解）

1. **Trace vs Score vs Dataset**：问 "Trace、Score、Dataset 三者的关系是什么？各自解决什么问题？"（期望：Trace 是单次执行的详细记录，Score 是聚合指标，Dataset 是结构化数据集用于评估）
2. **Prompt 版本管理**：问 "生产环境出问题了，怎么快速回滚 Prompt？"（期望：在 Langfuse Hub 把上一版打上 production 标签，应用自动拉取）
3. **缓存策略权衡**：问 "TTL 设太长和太短各有什么问题？"（期望：太长→数据过期；太短→缓存命中率低、API 费用高）

---

### 4. 常见误区与注意事项

| 坑 | 现象 | 预警话术 |
|----|------|---------|
| **Langfuse 没配环境变量** | 追踪数据没上报 | "Langfuse 的 `secret_key` 和 `public_key` 必须配好，且不同环境用不同 project。" |
| **Prompt 本地和 Hub 不一致** | 生产环境用了旧版 Prompt | "上线前必须确认 Prompt 同步状态灯是绿色。黄色=待推送，红色=Hub 有新版本待拉取。" |
| **缓存雪崩** | 大量缓存同时过期，瞬间打爆 API | "TTL 加随机偏移（±10%），避免同时过期；热点数据用互斥锁防穿透。" |
| **RAG 检索质量差** | 检索结果不相关，影响生成质量 | "RAG 效果要持续评估：命中率、相关性评分、Bad Case 收集。检索不行，生成肯定不行。" |
| **状态太大导致性能下降** | Checkpoint 保存/恢复越来越慢 | "定期裁剪 State，大文件（如图片）存对象存储，State 里只存引用。" |
| **忘记区分环境** | 开发环境的 Trace 混到生产环境 | "Tags 里必须打 env:dev/test/prod，Langfuse 项目也要分开。" |

#### 调试技巧

1. **Langfuse Trace 排查**：找到慢请求 → 看瀑布图找瓶颈节点 → 优化该节点
2. **Prompt 版本对比**：本地 vs Hub 并排对比，快速定位差异
3. **缓存效果验证**：对比「有缓存」和「无缓存」的响应时间和成本
4. **RAG 效果评估**：定期跑评测集，看命中率和相关性趋势

---

### 5. 阶段小结

#### 核心要点回顾

1. **Langfuse Trace = X 光片**：每次请求的完整执行链路，精准定位问题
2. **Langfuse Score = 体检报告**：缓存命中率、响应时间、输出质量的聚合指标
3. **Langfuse Dataset = 病历本**：自动采集的 QA 数据，用于回归测试和效果评估
4. **Prompt YAML + Hub = 菜谱管理**：本地开发 → 云端版本管理 → 生产自动同步
5. **混合架构 = 速度与安全的平衡**：Redis 热数据 + Postgres 冷数据
6. **生产部署 = 全流程**：Docker 容器化、健康检查、性能调优、安全加固

#### 记忆口诀

> **"Trace 看链路，Score 看指标，Dataset 做评估；YAML 管本地，Hub 管云端，同步保一致。"**

---

### 6. 与课程结束的衔接

> "八节课，我们从一张最简单的 StateGraph 开始，走到今天这个生产级的 OpsMate——有路由、有并行、有记忆、有人机协作、有流式输出、有多智能体、有可观测性、有 Prompt 生命周期管理。
>
> 你们现在掌握的技能：
> - 能根据业务复杂度判断预构建 vs 自定义的选型
> - 能设计并行的子图结构，处理复杂工作流
> - 能构建带 HITL 的安全可控系统
> - 能实现 DeepResearch 级别的复杂 Agent
> - 能部署到生产环境，用 Langfuse 监控全链路
> - 能管理 Prompt 版本，实现开发到生产的无缝同步
>
> 课程的终点是你们项目的起点。去构建吧！"

---

---

## 附录：教学通用建议

### 每节课的推荐时间分配

| 环节 | 时长 | 说明 |
|------|------|------|
| 开场引入 | 5-10 min | 激发兴趣，建立上下文 |
| 概念讲解 | 90-180 min | 核心知识，建议每 20 min 休息一次 |
| 代码演示 | 30-60 min | 边写边讲，学员同步跟敲 |
| 案例实战 | 60-150 min | 学员动手，讲师巡回指导 |
| 阶段小结 | 5 min | 要点回顾，记忆口诀 |
| 课后练习 | 课后完成 | 3-5 道编程题 + 1 道设计题 |

### 画图工具建议

- **ASCII 图**：适合快速板书、代码注释中嵌入
- **Mermaid 语法**：适合讲义文档、可渲染成流程图
- **LangGraph Studio**：适合动态展示图执行过程（强烈推荐）
- **Excalidraw**：适合手绘风格的架构图

### 代码演示最佳实践

1. **先展示最终效果**：让学员知道「我们要做成什么样」
2. **从空文件开始写**：不要直接贴完整代码，边写边讲
3. **故意留 bug**：让学员指出问题，加深理解
4. **每完成一个节点就运行一次**：验证增量成果，及时发现问题
5. **保存 checkpoint**：每节课保存代码快照，方便学员回顾

### 学员提问应对策略

| 问题类型 | 应对策略 |
|----------|----------|
| "这个和 LangChain 有什么区别？" | 用对比图展示 LangChain（链）vs LangGraph（图） |
| "为什么要这么复杂？" | 先展示简单方案的局限性，再引出复杂方案的必要性 |
| "生产环境真的这么用吗？" | 引用 LangGraph 官方案例或知名开源项目 |
| "这个 bug 怎么排查？" | 引导学员用 `print(state)`、`stream()`、`LangGraph Studio` 逐步定位 |

---

*本文档为 LangGraph 课程的教学逻辑大纲，与 course-roadmap.md 配套使用。实际授课时可根据学员反馈和进度灵活调整。*
